include ../parts/mixins

.mdl-grid
  - cardSettings = { phone: { s: 4 }, tablet: { s: 4, o: 2 }, desktop: { s: 6, o: 4 } }
  
  if query_params.error
    +MessageCard(cardSettings, 'Ошибка при создании специальности', query_params.error)
  
  //- var selected_type = query_params.user_type || 'student';

  +Form(cardSettings, 'createspecialtyform', '/a/specialties/create')
    +InputField('Название', 'name', true)